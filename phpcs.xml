<?xml version="1.0"?>
<ruleset name="TheMatra coding standard">
    <description>TheMatra coding standard.</description>

    <file>./src</file>

    <exclude-pattern>*\.(?!php$)</exclude-pattern>

    <arg name="basepath" value="."/>
    <arg name="colors" />
    <arg value="p"/>

    <!-- Include the whole PSR2 standard -->
    <rule ref="PSR2">
        <!-- Override PSR2 The soft limit on line length MUST be 120 characters; -->
        <exclude name="Generic.Files.LineLength"/>
    </rule>
</ruleset>